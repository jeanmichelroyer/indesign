# Script - redefineScalingAs100

| Redefinir la mise à l'échelle à 100% des éléments.| Reset the scaling to 100% for elements.|
| Si objet sélectionné, n'agit que sur lui. | If the object is selected, it only acts on that object. |
